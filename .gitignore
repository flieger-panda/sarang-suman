# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Personal working notes — plans, checklists, scratch. Local only, not part of
# the project. The invariants any of this produced that a contributor actually
# needs are written into CLAUDE.md and website_design.md instead, so nothing
# tracked depends on a file in here existing.
personal/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
