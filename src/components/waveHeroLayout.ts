export const LINE_COUNT = 32;
export const CHARS_PER_LINE = 80;
export const NAME = "sarang suman";
export const CENTER_ROW_INDEX = Math.floor(LINE_COUNT / 2) - 3;
export const NAME_START = Math.floor((CHARS_PER_LINE - NAME.length) / 2);
