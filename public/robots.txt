User-agent: *
Allow: /

# Generated by scripts/build-seo.mjs on every build, so new content/*.md files
# show up without touching this file. It carries the routes the home menu can't
# link to crawlably: the submenu rows (skills, projects, resume, music) only
# exist in the DOM once expanded, so the sitemap is how they get discovered.
Sitemap: https://sarangsuman.me/sitemap.xml
