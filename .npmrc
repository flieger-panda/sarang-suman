# Makes package.json's "engines" field enforced rather than advisory. Without
# this npm only prints an EBADENGINE *warning* and installs anyway, so a wrong
# Node version surfaces later as a confusing downstream failure instead of an
# explicit one. See the Deployment section of CLAUDE.md.
engine-strict=true
